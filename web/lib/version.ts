/**
 * Versão atual publicada do FragReel client (Windows .exe).
 *
 * Atualizar aqui sempre que tagear nova release no fragreel-client.
 * O download em si é dinâmico via /releases/latest/download/, mas
 * exibimos esta string nos CTAs de download para o usuário poder
 * confirmar visualmente qual versão está pegando (e dar bypass de
 * cache do navegador quando muda).
 */
export const CLIENT_VERSION = "v0.2.11";
